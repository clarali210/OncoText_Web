import { Meteor } from 'meteor/meteor';

import '../imports/api/reports.js';
import '../imports/api/annotations.js';
import '../imports/api/episodes.js';

Meteor.startup(() => {
  // code to run on server at startup
});
